# Signal Peptide Efficiency Prediction

I predict signal-peptide secretion efficiency (weighted average, **WA**, of the activity-bin distribution, on a 1–10 scale)
in *Bacillus subtilis* from the [Grasso et al. 2023](https://doi.org/10.1021/acssynbio.2c00328)
library, comparing random forests and neural networks across physicochemical descriptors and
several protein-language-model (PLM) embeddings. Predicting the full 10-bin probability
distribution (softmax → WA) rather than a scalar, with multi-seed ensembling, gives a
**reproducible test MSE of ≈ 0.98**, about a **20% improvement over the Grasso RF baseline
(1.22)** on the official test split.

> Numbers are reported as mean ± std over retrains (this architecture is high-variance, ≈ ±0.05 per single model); see [`docs/reproducibility_findings.md`](docs/reproducibility_findings.md) for details.

## Key result: a *reproducible* open-weight headline

The best single embedding, Ginkgo AA-0, is **API-only, proprietary, and the API has been
discontinued**; its embeddings can no longer be regenerated by anyone, so it cannot anchor a
reproducible result (see [Reproducibility](#reproducibility-and-the-ginkgo-aa-0-caveat)). I
therefore base the headline on **open-weight models**, and recover essentially all of AA-0's
performance with an ensemble of architecturally diverse open PLMs:

| Model (vector NN, 5-seed, mean ± std over retrains) | Test MSE | Reproducible? |
|---|---|---|
| **ProtT5 + ESM2-650M + ProtBERT (open ensemble)** | **0.981 ± 0.005** | ✅ open weights |
| ProtT5 + ProtBERT | 0.986 | ✅ |
| ProtT5 + ESM2-650M | 0.995 | ✅ |
| ProtT5 (alone) | 1.017 | ✅ |
| ESM2-650M (alone) | 1.072 | ✅ |
| ProtBERT (alone) | 1.084 | ✅ |
| Ginkgo AA-0 (alone) | 0.959 ± 0.014 | ❌ API discontinued |
| Grasso et al. RF (baseline) | 1.22 | — |

The open ensemble (**0.981**) closes the gap to the now-unavailable AA-0 (0.959) to within ~0.02
and improves ~20% over Grasso's 1.22, and **anyone can regenerate every embedding from
HuggingFace, no API key, no defunct service.** The ensemble works because ProtT5 is strong
enough (1.017) that the three open models are quality-comparable, so averaging their
decorrelated errors yields a genuine improvement; earlier ensembling *failed* when one embedding (AA-0)
so dominated the others that averaging only diluted it.

## Generalization: leave-one-gene-out

The Grasso train/test split shares **132 of 134 genes** (≈ 99.8% of test sequences) between
train and test, so the standard test MSE is optimistic. Under **leave-one-gene-out
cross-validation**, MSE rises to **2.19** (Spearman 0.72). This is the realistic cross-gene
generalization estimate and should be read alongside the standard number, not behind it.

## What did *not* help

A systematic search for improvements (no test-set tuning) found that the focal-loss vector NN
is hard to beat per embedding:

| Direction tried | Outcome |
|---|---|
| Single-embedding cross-rep (Ginkgo + ESM2) | worse; dominant embedding diluted |
| Ordinal Cramér/CDF-L2 loss | worse than focal |
| Focal + λ·MSE(WA) hybrid | worse than focal |
| Non-neural (HistGBM, SVR, Ridge, RF) | all worse than the NN |
| ProtBERT as a single embedding | not better than ESM2 |
| PReLU activation (matching the reference) | **helped (~0.02); adopted** |
| **Open-model ensemble** (balanced components) | **helped; the headline above** |

## Methodology

| # | Script | Approach |
|---|--------|----------|
| 00 | Generate design embeddings | ESM2-650M embeddings for design variants |
| 01 | Baseline reproduction | Grasso RF reproduction from the `Set` column |
| 02 | RF hyperparameter search | Independent RandomizedSearchCV per feature type |
| 03 | NN regression search | Per-embedding NN search (scaler fit on train partition only) |
| 04 | Final comparison | Grouped bar chart (axis from 0) + CSV |
| 05 | Cross-dataset generalization | Zero-shot transfer to 4 external SP datasets |
| 06 | Vector regression | 10-bin softmax → WA, focal/CE loss |
| 07 | Design-task evaluation | Mutation-effect ranking + Grasso Fig 4e (±1 WA) comparison |
| 08 | Bootstrap CIs | 10k-resample 95% CIs |
| 09–11 | Vector optimization & validation | Architecture, dropout (validation-selected), reproducibility |
| 12 | Bimodal histogram | Motivating figure (single-column, Tufte) |
| 13 | Cross-dataset fine-tuning | Pooled out-of-fold Spearman + bootstrap CI |
| 14–20 | Controls | ReLU², output formulation, unified schema, leave-one-gene-out, linear baselines, ablation, ESM2 optimization |
| 21 | Open-embedding ensemble | ProtT5 / ProtBERT generation (`gen_open_embeddings.py`) + the reproducible headline ensemble |
| — | `design_parity_fig4e.py` | Design-task ±1-WA fraction + parity plot vs Grasso Fig 4e |

(Reproducibility/diagnostic experiments (seed-distribution measurement, the Wolfram
cross-check, and the improvement search) are documented in
[`docs/reproducibility_findings.md`](docs/reproducibility_findings.md).)

## Reproducibility and the Ginkgo AA-0 caveat

- **Ginkgo AA-0 is discontinued and unreproducible.** Its weights were never released (API-only,
  proprietary); the model API and registration portal are gone. I report AA-0 (~0.96) only as a
  caveated comparison and base all reproducible results on open weights. A linear
  probe confirms AA-0 and ESM2-650M capture essentially the same signal (Pearson r = 0.975).
- **This architecture is high-variance**, so every headline is reported as mean ± std over
  retrains rather than a single run. Re-training the (256,256,128)/dropout-0.35 config gives
  0.957 ± 0.009; the Wolfram net4 prototype behaves the same, with single runs spanning
  0.94–1.07 (0.99 ± 0.046 over 8 seeds).
- **No hyperparameter is selected on the test set** in the reproducible pipeline; the scaler is
  fit on the training partition only; the exact Grasso `Set` column defines train/test (full
  1326-row test set, no deduplication).

## Setup & running

```bash
pip install -r requirements.txt          # numpy, pandas, scikit-learn, tensorflow, scipy, pyarrow
# open-weight embedding generation (Script 21) additionally needs: torch, transformers, sentencepiece
```

All data files are included in `data/`. Scripts write JSON/CSV to `results/` and 300-DPI PNGs to
`figures/`. `run_all.ipynb` runs the pipeline end-to-end with explanatory cells.

## Data

`data/sb2c00328_si_011.xlsx` (Grasso library: 156 physicochemical features, WA, and 10 bin
probabilities per sequence; the `Set` column defines the official train/test split) plus
precomputed PLM-embedding parquets (ESM2-650M/3B, the discontinued Ginkgo AA-0, and the
open-weight ProtT5 / ProtBERT embeddings generated here). External datasets (Wu, Xue, Zhang) use
ESM2-650M embeddings. See [`docs/reproducibility_findings.md`](docs/reproducibility_findings.md)
for the complete audit and decision record.
