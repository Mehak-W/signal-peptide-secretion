# Core pipeline (Scripts 01-19)
numpy>=1.23
pandas>=1.5
scikit-learn>=1.2
scipy>=1.10
matplotlib>=3.6
openpyxl>=3.0
pyarrow>=10.0
tensorflow>=2.12

# Open-weight PLM embedding generation (Script 21 / scripts/gen_open_embeddings.py)
# ProtT5 and ProtBERT load from HuggingFace -- no API key, fully reproducible.
# transformers is pinned <5 because 5.x dropped the legacy ProtT5/ProtBERT
# sentencepiece tokenizers used here.
torch>=2.0
transformers>=4.40,<5.0
sentencepiece>=0.1.99
